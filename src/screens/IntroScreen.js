import React from 'react';
import { View } from 'react-native';

// This is a dummy fallback for native.
// The real IntroScreen is only meant for web (IntroScreen.web.js)
export default function IntroScreen() {
  return <View />;
}
